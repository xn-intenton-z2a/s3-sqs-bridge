# .github/workflows/agent-flow-feature-maintenance.yml
#
# This file is part of the example suite for `agentic-lib` see: https://github.com/xn-intenton-z2a/agentic-lib
# This file is licensed under the MIT License. For details, see LICENSE-MIT

name: flow-feature-maintenance
concurrency: agentic-lib-commit-feature
run-name: "flow feature maintenance"

on:
  #push:
  #  paths:
  #    - '**/*.yml'
  workflow_dispatch:
    inputs:
      #mission:
      #  description: 'Text to drive the mission creation (if "house choice", the repository will be assessed and an action chosen). e.g. "Support output to PDF."'
      #  required: false
      #  type: string
      #  default: 'house choice'
      feature:
        description: 'Text to drive the feature creation (if "house choice", the repository will be assessed and an action chosen). e.g. "Support output to PDF."'
        required: false
        type: string
        default: 'house choice'
  #workflow_run:
  #  workflows:
  #    - seed-repository
  #  types:
  #    - completed
  workflow_call:
    inputs:
      #mission:
      #  description: 'Text to drive the mission creation (if "house choice", the repository will be assessed and an action chosen). e.g. "Support output to PDF."'
      #  required: false
      #  type: string
      #  default: 'house choice'
      feature:
        description: 'Text to drive the feature creation (if "house choice", the repository will be assessed and an action chosen). e.g. "Support output to PDF."'
        required: false
        type: string
        default: 'house choice'
    secrets:
      PERSONAL_ACCESS_TOKEN:
        required: false
      CHATGPT_API_SECRET_KEY:
        required: true
  schedule:
    - cron: '54 6 */28 * *' # schedule-1
    #- cron: '54 */4 * * *' # schedule-2
    #- cron: '24,54 * * * *' # schedule-3
    #- cron: '54 * * * *' # schedule-4

env:
  npmAuthOrganisation: ${{ vars.npmAuthOrganisation || '@xn-intenton-z2a' }}
  s3BucketUrl: ${{ vars.s3BucketUrl || 's3://agentic-lib-telemetry-bucket/events/' }}
  s3WebsiteBucketUrl: ${{ vars.s3WebsiteBucketUrl || 's3://agentic-lib-public-website-stats-bucket/' }}
  iamActionsRoleArn: ${{ vars.iamActionsRoleArn || 'arn:aws:iam::541134664601:role/agentic-lib-github-actions-role' }}
  iamStatsBucketWriterRoleArn: ${{ vars.iamStatsBucketWriterRoleArn || 'arn:aws:iam::541134664601:role/agentic-lib-deployment-role' }}
  iamPublicWebsiteStatsBucketWriterRoleArn: ${{ vars.iamPublicWebsiteStatsBucketWriterRoleArn || 'arn:aws:iam::541134664601:role/agentic-lib-public-website-stats-bucket-writer-role' }}

jobs:

  check-secrets:
    runs-on: ubuntu-latest
    steps:
      - name: Check if CHATGPT_API_SECRET_KEY is available
        run: |
          if [ -z "${{ secrets.CHATGPT_API_SECRET_KEY }}" ]; then
            echo "::error::CHATGPT_API_SECRET_KEY is not available. Stopping workflow."
            exit 1
          fi
          echo "CHATGPT_API_SECRET_KEY is available."

  agentic-lib:
    needs:
      - check-secrets
    uses: 'xn-intenton-z2a/agentic-lib/.github/workflows/wfr-agent-config.yml@6.6.1'
    with:
      configPath: ${{ vars.configPath || '.github/agents/agentic-lib.yml' }}
    secrets:
      PERSONAL_ACCESS_TOKEN: ${{ secrets.PERSONAL_ACCESS_TOKEN }}

  # GitHub Script job which checks if the target path starts with one of writeableFilepaths and fails out if not
  check-writeable-filepaths:
    needs:
      - agentic-lib
    runs-on: ubuntu-latest
    steps:
      - name: Check if target is writeable
        id: check-writeable-filepaths
        uses: actions/github-script@v7
        env:
          targetFilepath: ${{ needs.agentic-lib.outputs.featuresPath }}
          writeableFilepaths: ${{ needs.agentic-lib.outputs.writeableFilepaths }}
        with:
          script: |
            const writeableFilepaths = process.env.writeableFilepaths.split(';');
            let targetFilepath;
            targetFilepath = process.env.targetFilepath;
            if (!writeableFilepaths.some(path => targetFilepath.startsWith(path))) {
              core.setFailed(`targetFilepath "${targetFilepath}" is not in the list of writeable filepaths.`);
              return;
            } else {
              core.info(`targetFilepath "${targetFilepath}" is in the list of writeable filepaths.`);
            }

  # If the feature is "house choice", the repository will be assessed an action chosen, otherwise the supplied text will be used to generate a new feature.
  #maintain-sources:
  #  needs:
  #    - agentic-lib
  #    - check-writeable-filepaths
  #  permissions:
  #    contents: write
  #    issues: write
  #    id-token: write
  #  uses: 'xn-intenton-z2a/agentic-lib/.github/workflows/wfr-completion-maintain-sources.yml@6.6.1'
  #  with:
  #    promptFilepath: '.github/agents/agent-maintain-sources.md'
  #    agentConfigContent: ${{ needs.agentic-lib.outputs.agentConfigContent }}
  #    mission: ${{ inputs.mission }}
  #    missionFilepath: ${{ needs.agentic-lib.outputs.missionFilepath }}
  #    librarySourcesFilepath: ${{ needs.agentic-lib.outputs.librarySourcesFilepath }}
  #    librarySourcesLimit: '${{needs.agentic-lib.outputs.librarySourcesLimit }}'
  #    contributingFilepath: ${{ needs.agentic-lib.outputs.contributingFilepath }}
  #    allTestsPaths: ${{ needs.agentic-lib.outputs.allTestsPaths }}
  #    allSourcePaths: ${{ needs.agentic-lib.outputs.allSourcePaths }}
  #    dependenciesFilepath: ${{ needs.agentic-lib.outputs.dependenciesFilepath }}
  #    documentationPath: ${{ needs.agentic-lib.outputs.documentationPath }}
  #    readmeFilepath: ${{ needs.agentic-lib.outputs.readmeFilepath }}
  #    writeableFilepaths: ${{ needs.agentic-lib.outputs.writeableFilepaths }}
  #    model: ${{ vars.CHATGPT_API_MODEL || 'o4-mini' }}
  #    npmAuthOrganisation: ${{ vars.npmAuthOrganisation || '@xn-intenton-z2a' }}
  #    gitUserEmail: 'action@github.com'
  #    gitUserName: 'GitHub Actions[bot]'
  #  secrets:
  #    CHATGPT_API_SECRET_KEY: ${{ secrets.CHATGPT_API_SECRET_KEY }}
  #    PERSONAL_ACCESS_TOKEN: ${{ secrets.PERSONAL_ACCESS_TOKEN }}

  # If the document is "house choice", the repository will be assessed and an action chosen otherwise the supplied text will be used to generate a new document.
  #maintain-library:
  #  needs:
  #    - agentic-lib
  #    - check-writeable-filepaths
  #    - maintain-sources
  #  if: ${{ !cancelled() }}
  #  permissions:
  #    contents: write
  #    issues: write
  #    id-token: write
  #  uses: 'xn-intenton-z2a/agentic-lib/.github/workflows/wfr-completion-maintain-library.yml@6.6.1'
  #  with:
  #    promptFilepath: '.github/agents/agent-maintain-library.md'
  #    agentConfigContent: ${{ needs.agentic-lib.outputs.agentConfigContent }}
  #    librarySourcesFilepath: '${{ needs.agentic-lib.outputs.librarySourcesFilepath }}'
  #    libraryDocumentsPath: '${{ needs.agentic-lib.outputs.libraryDocumentsPath }}'
  #    libraryDocumentsLimit: '${{ needs.agentic-lib.outputs.libraryDocumentsLimit }}'
  #    writeableFilepaths: ${{ needs.agentic-lib.outputs.writeableFilepaths }}
  #    model: ${{ vars.CHATGPT_API_MODEL || 'o4-mini' }}
  #    npmAuthOrganisation: ${{ vars.npmAuthOrganisation || '@xn-intenton-z2a' }}
  #    gitUserEmail: 'action@github.com'
  #    gitUserName: 'GitHub Actions[bot]'
  #  secrets:
  #    CHATGPT_API_SECRET_KEY: ${{ secrets.CHATGPT_API_SECRET_KEY }}
  #    PERSONAL_ACCESS_TOKEN: ${{ secrets.PERSONAL_ACCESS_TOKEN }}

  # If the feature is "house choice", the repository will be assessed an action chosen otherwise the supplied text will be used to generate a new feature.
  maintain-features:
    needs:
      - agentic-lib
      - check-writeable-filepaths
      #- maintain-library
    if: ${{ !cancelled() }}
    permissions:
      contents: write
      issues: write
      id-token: write
    uses: 'xn-intenton-z2a/agentic-lib/.github/workflows/wfr-completion-maintain-features.yml@6.6.1'
    with:
      promptFilepath: '.github/agents/agent-maintain-features.md'
      agentConfigContent: ${{ needs.agentic-lib.outputs.agentConfigContent }}
      feature: ${{ inputs.feature }}
      featuresPath: ${{ needs.agentic-lib.outputs.featuresPath }}
      featuresLimit: '${{ needs.agentic-lib.outputs.featuresLimit }}'
      allSourcePaths: ${{ needs.agentic-lib.outputs.allSourcePaths }}
      allTestsPaths: ${{ needs.agentic-lib.outputs.allTestsPaths }}
      documentationPath: ${{ needs.agentic-lib.outputs.documentationPath }}
      readmeFilepath: ${{ needs.agentic-lib.outputs.readmeFilepath }}
      missionFilepath: ${{ needs.agentic-lib.outputs.missionFilepath }}
      contributingFilepath: ${{ needs.agentic-lib.outputs.contributingFilepath }}
      dependenciesFilepath: ${{ needs.agentic-lib.outputs.dependenciesFilepath }}
      writeableFilepaths: ${{ needs.agentic-lib.outputs.writeableFilepaths }}
      model: ${{ vars.CHATGPT_API_MODEL || 'o4-mini' }}
      npmAuthOrganisation: ${{ vars.npmAuthOrganisation || '@xn-intenton-z2a' }}
      gitUserEmail: 'action@github.com'
      gitUserName: 'GitHub Actions[bot]'
    secrets:
      CHATGPT_API_SECRET_KEY: ${{ secrets.CHATGPT_API_SECRET_KEY }}
      PERSONAL_ACCESS_TOKEN: ${{ secrets.PERSONAL_ACCESS_TOKEN }}

  log-intention-activity-maintain-features:
    needs:
      - agentic-lib
      - maintain-features
    runs-on: ubuntu-latest
    env:
      gitUserEmail: 'action@github.com'
      gitUserName: 'GitHub Actions[bot]'
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: ${{ github.ref }}

      - name: Get latest from remote
        run: |
          git config --local user.email "${{ env.gitUserEmail }}"
          git config --local user.name "${{ env.gitUserName }}"
          git pull --ff-only origin ${{ github.ref }}

      - name: log-intention-activity
        id: log-intention-activity
        uses: actions/github-script@v7
        env:
          activity: "Maintained feature ${{ needs.maintain-features.outputs.featureName }}."
          seedDiscussionFilepath: ${{ needs.agentic-lib.outputs.seedDiscussionFilepath }}
        with:
          script: |
            const activity = process.env.activity;
            const seedDiscussionFilepath = process.env.seedDiscussionFilepath;

            core.info(`Activity: ${activity}`);
            core.info(`Seed discussion filepath: ${seedDiscussionFilepath}`);

            const fs = require('fs');
            const path = require('path');

            // Create trace file and the parent directory of seedDiscussionFilepath if it doesn't exist
            if (!fs.existsSync(path.dirname(seedDiscussionFilepath))) {
              fs.mkdirSync(path.dirname(seedDiscussionFilepath), { recursive: true });
            }
            const isoDate = new Date().toISOString();
            const activityLogContent = `${isoDate} - ${activity}\n\n`;
            fs.appendFileSync(seedDiscussionFilepath, activityLogContent);
            core.info(`Activity logged to ${seedDiscussionFilepath}`);

      - name: Commit changes
        run: |
          git config --local user.email "${{ env.gitUserEmail }}"
          git config --local user.name "${{ env.gitUserName }}"
          git add  ${{ needs.agentic-lib.outputs.seedDiscussionFilepath }}
          git commit -m "Activity logged" || echo "No changes to commit"
          git push

  generate-feature-development-issue:
    needs:
      - agentic-lib
      - maintain-features
    if: ${{ !cancelled() }}
    permissions:
      contents: write
      issues: write
      id-token: write
    uses: 'xn-intenton-z2a/agentic-lib/.github/workflows/wfr-completion-generate-feature-development-issue.yml@6.6.1'
    with:
      promptFilepath: '.github/agents/agent-feature-issue.md'
      agentConfigContent: ${{ needs.agentic-lib.outputs.agentConfigContent }}
      featureName: ${{ needs.maintain-features.outputs.featureName }}
      featuresPath: ${{ needs.agentic-lib.outputs.featuresPath }}
      libraryDocumentsPath: ${{ needs.agentic-lib.outputs.libraryDocumentsPath }}
      featureDevelopmentIssuesWipLimit: '${{ needs.agentic-lib.outputs.featureDevelopmentIssuesWipLimit }}'
      allSourcePaths: ${{ needs.agentic-lib.outputs.allSourcePaths }}
      allTestsPaths: ${{ needs.agentic-lib.outputs.allTestsPaths }}
      documentationPath: ${{ needs.agentic-lib.outputs.documentationPath }}
      readmeFilepath: ${{ needs.agentic-lib.outputs.readmeFilepath }}
      missionFilepath: ${{ needs.agentic-lib.outputs.missionFilepath }}
      contributingFilepath: ${{ needs.agentic-lib.outputs.contributingFilepath }}
      dependenciesFilepath: ${{ needs.agentic-lib.outputs.dependenciesFilepath }}
      buildScript: ${{ needs.agentic-lib.outputs.buildScript }}
      testScript: ${{ needs.agentic-lib.outputs.testScript }}
      mainScript: ${{ needs.agentic-lib.outputs.mainScript }}
      writeableFilepaths: ${{ needs.agentic-lib.outputs.writeableFilepaths }}
      issueSelectionLabel: 'feature'
      model: ${{ vars.CHATGPT_API_MODEL || 'o4-mini' }}
      npmAuthOrganisation: ${{ vars.npmAuthOrganisation || '@xn-intenton-z2a' }}
    secrets:
      CHATGPT_API_SECRET_KEY: ${{ secrets.CHATGPT_API_SECRET_KEY }}
      PERSONAL_ACCESS_TOKEN: ${{ secrets.PERSONAL_ACCESS_TOKEN }}

  log-intention-activity:
    needs:
      - agentic-lib
      - generate-feature-development-issue
    runs-on: ubuntu-latest
    env:
      gitUserEmail: 'action@github.com'
      gitUserName: 'GitHub Actions[bot]'
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: ${{ github.ref }}

      - name: Get latest from remote
        run: |
          git config --local user.email "${{ env.gitUserEmail }}"
          git config --local user.name "${{ env.gitUserName }}"
          git pull --ff-only origin ${{ github.ref }}

      - name: log-intention-activity
        id: log-intention-activity
        uses: actions/github-script@v7
        env:
          activity: "Generated feature development issue with title ${{ needs.generate-feature-development-issue.outputs.issueTitle }}."
          seedDiscussionFilepath: ${{ needs.agentic-lib.outputs.seedDiscussionFilepath }}
        with:
          script: |
            const activity = process.env.activity;
            const seedDiscussionFilepath = process.env.seedDiscussionFilepath;

            core.info(`Activity: ${activity}`);
            core.info(`Seed discussion filepath: ${seedDiscussionFilepath}`);

            const fs = require('fs');
            const path = require('path');

            // Create trace file and the parent directory of seedDiscussionFilepath if it doesn't exist
            if (!fs.existsSync(path.dirname(seedDiscussionFilepath))) {
              fs.mkdirSync(path.dirname(seedDiscussionFilepath), { recursive: true });
            }
            const isoDate = new Date().toISOString();
            const activityLogContent = `${isoDate} - ${activity}\n\n`;
            fs.appendFileSync(seedDiscussionFilepath, activityLogContent);
            core.info(`Activity logged to ${seedDiscussionFilepath}`);

      - name: Commit changes
        run: |
          git config --local user.email "${{ env.gitUserEmail }}"
          git config --local user.name "${{ env.gitUserName }}"
          git add  ${{ needs.agentic-lib.outputs.seedDiscussionFilepath }}
          git commit -m "Activity logged" || echo "No changes to commit"
          git push

  create-issue:
    needs:
      - generate-feature-development-issue
    permissions:
      issues: write
      id-token: write
    uses: 'xn-intenton-z2a/agentic-lib/.github/workflows/wfr-github-create-issue.yml@6.6.1'
    with:
      issueTitle: ${{ needs.generate-feature-development-issue.outputs.issueTitle }}
      issueBody: ${{ needs.generate-feature-development-issue.outputs.issueDescription }}
      issueLabels: 'feature, automated'

  select-issue-to-enhance:
    needs:
      - create-issue
    if: ${{ !cancelled() }}
    permissions:
      issues: write
      id-token: write
    uses: 'xn-intenton-z2a/agentic-lib/.github/workflows/wfr-github-select-issue.yml@6.6.1'
    with:
      issueNumber: ${{ needs.create-issue.outputs.issueNumber }}
      selectionLabel: 'automated'
      filterLabel: 'in-progress'
    secrets:
      CHATGPT_API_SECRET_KEY: ${{ secrets.CHATGPT_API_SECRET_KEY }}

  enhance-issue:
    needs:
      - create-issue
      - agentic-lib
      - select-issue-to-enhance
    if: ${{ !cancelled() }}
    permissions:
      contents: read
      issues: write
      id-token: write
    uses: 'xn-intenton-z2a/agentic-lib/.github/workflows/wfr-completion-enhance-issue.yml@6.6.1'
    with:
      promptFilepath: '.github/agents/agent-ready-issue.md'
      agentConfigContent: ${{ needs.agentic-lib.outputs.agentConfigContent }}
      issueNumber: ${{ needs.select-issue-to-enhance.outputs.issueNumber }}
      featuresPath: ${{ needs.agentic-lib.outputs.featuresPath }}
      libraryDocumentsPath: ${{ needs.agentic-lib.outputs.libraryDocumentsPath }}
      allSourcePaths: ${{ needs.agentic-lib.outputs.allSourcePaths }}
      allTestsPaths: ${{ needs.agentic-lib.outputs.allTestsPaths }}
      documentationPath: ${{ needs.agentic-lib.outputs.documentationPath }}
      readmeFilepath: ${{ needs.agentic-lib.outputs.readmeFilepath }}
      missionFilepath: ${{ needs.agentic-lib.outputs.missionFilepath }}
      contributingFilepath: ${{ needs.agentic-lib.outputs.contributingFilepath }}
      dependenciesFilepath: ${{ needs.agentic-lib.outputs.dependenciesFilepath }}
      buildScript: ${{ needs.agentic-lib.outputs.buildScript }}
      testScript: ${{ needs.agentic-lib.outputs.testScript }}
      mainScript: ${{ needs.agentic-lib.outputs.mainScript }}
      writeableFilepaths: ${{ needs.agentic-lib.outputs.writeableFilepaths }}
      model: ${{ vars.CHATGPT_API_MODEL || 'o4-mini' }}
      npmAuthOrganisation: "${{ vars.npmAuthOrganisation || '@xn-intenton-z2a' }}"
    secrets:
      CHATGPT_API_SECRET_KEY: ${{ secrets.CHATGPT_API_SECRET_KEY }}
      PERSONAL_ACCESS_TOKEN: ${{ secrets.PERSONAL_ACCESS_TOKEN }}

  log-intention-activity-enhance-issue:
    needs:
      - agentic-lib
      - enhance-issue
      - select-issue-to-enhance
    runs-on: ubuntu-latest
    env:
      gitUserEmail: 'action@github.com'
      gitUserName: 'GitHub Actions[bot]'
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: ${{ github.ref }}

      - name: Get latest from remote
        run: |
          git config --local user.email "${{ env.gitUserEmail }}"
          git config --local user.name "${{ env.gitUserName }}"
          git pull --ff-only origin ${{ github.ref }}

      - name: log-intention-activity
        id: log-intention-activity
        uses: actions/github-script@v7
        env:
          activity: "Enhanced issue ${{ needs.select-issue.outputs.issueNumber }}."
          seedDiscussionFilepath: ${{ needs.agentic-lib.outputs.seedDiscussionFilepath }}
        with:
          script: |
            const activity = process.env.activity;
            const seedDiscussionFilepath = process.env.seedDiscussionFilepath;

            core.info(`Activity: ${activity}`);
            core.info(`Seed discussion filepath: ${seedDiscussionFilepath}`);

            const fs = require('fs');
            const path = require('path');

            // Create trace file and the parent directory of seedDiscussionFilepath if it doesn't exist
            if (!fs.existsSync(path.dirname(seedDiscussionFilepath))) {
              fs.mkdirSync(path.dirname(seedDiscussionFilepath), { recursive: true });
            }
            const isoDate = new Date().toISOString();
            const activityLogContent = `${isoDate} - ${activity}\n\n`;
            fs.appendFileSync(seedDiscussionFilepath, activityLogContent);
            core.info(`Activity logged to ${seedDiscussionFilepath}`);

      - name: Commit changes
        run: |
          git config --local user.email "${{ env.gitUserEmail }}"
          git config --local user.name "${{ env.gitUserName }}"
          git add  ${{ needs.agentic-lib.outputs.seedDiscussionFilepath }}
          git commit -m "Activity logged" || echo "No changes to commit"
          git push
